# Local workstation install.
[workstation]
localhost ansible_connection=local

# To target remote machines instead, comment out the line above and add hosts:
# [workstation]
# devbox-01 ansible_host=10.0.0.21 ansible_user=ahill
